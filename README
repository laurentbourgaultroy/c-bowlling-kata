# Bowling Kata in C

The classical bowling Kata as stated by Uncle Bob. I was able to do the score
computation using recursive algorithm that I find rather elegant.

## How to run

Simply clone the git repo and `make` inside the project. You only need gcc
installed, no external library are required.
